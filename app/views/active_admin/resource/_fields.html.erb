<div class="observation-attributes">
  <% unchecked ||= [] %>
  <% attributes.each do |column| %>
    <div class="observations-checkbox" id="attribute-<%= column %>">
      <label>
        <input type="checkbox" class="observation-checkbox" id="<%= column %>" <%= unchecked.exclude?(column) ? 'checked' : '' %>>
        <%=column.humanize %>
      </label>
    </div>
  <% end %>
</div>
